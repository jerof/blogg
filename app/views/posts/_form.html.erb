<%= simple_form_for @post do |f| %>
  <%= f.input :title, autofocus: true %>
  <%= f.input :body, as: :text, autofocus: true, :input_html => { :style => 'width: 100%', :rows => 10 }%>
  <%= f.button :submit, class: "btn btn-lg btn-primary" %>
<% end %>
